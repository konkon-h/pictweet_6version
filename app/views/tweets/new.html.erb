<div class="contents row">
  <div class="container">
    <%= form_with(model: @tweet, local: true) do |form| %>  <!-- 渡されたモデルが空の場合createメソッドを呼び出す -->
      <h3>投稿する</h3>
      <%= render partial: "form", locals: { form: form } %>
    <% end %>
  </div>
</div>